const beforeEndElement = document.querySelector("main").insertAdjacentElement("beforeend", div);
const beforeBeginElement = document.querySelector("main").insertAdjacentElement("beforebegin", div);
const afterEndElement = document.querySelector("main").insertAdjacentElement("afterend", div);
const afterBeginElement = document.querySelector("main").insertAdjacentElement("afterbegin", div);